Django>=5.0,<6.0
inertia-django>=1.0
django-vite>=3.0
pyodbc>=5.1
# Backend Django untuk MS SQL — satu-satunya mesin basis data pangkal (akun,
# ServerProfile, TautanUser, log). Resmi Microsoft, lewat ODBC Driver 17/18.
#
# Batas atas `<2` disengaja: 2.0 mengganti lapisan drivernya (membawa
# `mssql-python`), dan seluruh test aplikasi ini — 1.027 per 2026-09-23 — hanya
# pernah dijalankan di atas 1.8. Tanpa batas ini, `pip install` di mesin baru
# memasang versi yang belum pernah diuji di sini; itu sudah terjadi sekali saat
# pemasangan produksi 2026-09-22 dan harus diturunkan tangan.
mssql-django>=1.5,<2
bcrypt>=4.1
cryptography>=42.0
python-dotenv>=1.0
waitress>=2.1.0
whitenoise>=6.6.0
openpyxl>=3.1
